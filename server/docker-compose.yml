version: '3.8'
services:
  climatus:
    build: .
    ports:
      - "12000:3000"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
